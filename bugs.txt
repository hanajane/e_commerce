BUGS

---you can switch up from a member to a guest by the url

---print user shipping info to checkout products

-----$searchText (search feature) doesnt work properly
	-couldnt fetch product types

--------doing the $userData ----done

------ i put null to yes in users table. put it back to no after the testing

-------home controller -- trying to fetch orders data to user profile